{
  "name": "@budget/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "delegate-mcp": "./dist/server.js"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist *.tsbuildinfo",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.3"
  }
}
